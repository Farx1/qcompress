'use client'

/**
 * Landing animations hook (no-op).
 * All animations are now handled by Framer Motion directly in components.
 */
export function useAnimeLanding() {
  // No-op: animations are handled by Framer Motion in components
  return
}


